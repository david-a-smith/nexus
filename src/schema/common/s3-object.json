{
  "title": "AWS S3 Object",
  "type": "object",
  "properties": {
    "bucket": {
      "$ref": "common/s3-bucket.json"
    },
    "object_key": {
      "type": "string",
      "title": "The key of the object in S3. Combine this with the bucket to access the object."
    }
  },
  "required": ["bucket", "object_key"]
}